# configuration file for building snapshots and releases with jitpack.io
jdk:
  - openjdk11
before_install:
  - ./custom-setup.sh
install:
  # This command just publishes the messaging package
  # - ./gradlew -PprojectsToPublish=":firebase-messaging" publishProjectsToMavenLocal
  # This command publishes all firebase packages
  - ./gradlew publishAllToLocal
env:
  ANDROID_NDK_HOME: "/opt/android-sdk-linux/ndk-bundle/21.4.7075529"
